# FaceSecureAttendance 小组分工说明

本项目为内容安全课程实践：**系统侧安全（鉴权、权限、数据边界）** 与 **生物特征侧安全（活体、比对、合照防伪）** 由两名后端同学分担；**前端集成与交互** 由一名同学负责。下列路径均以仓库根目录为起点。

---

## 总体分工一览

| 成员 | 角色定位 | 侧重 |
|------|-----------|------|
| **任家煜（A）** | 后端 + 数据库 + 系统安全基座 | 库表、JWT/角色、学生可见性、上传校验、认证与学生管理接口、导出与考勤列表侧权限 |
| **吴酽酌（B）** | 后端 + 识别链路 + 对抗安全 | 活体、人脸特征、合照匹配、情绪分析、打卡主流程与合照/情绪接口 |
| **巫威（C）** | 前端 + 联调与演示 | 全部 Vue 页面与路由、Axios 封装、摄像头与合照体验、对接后端契约 |

**协同说明：** `backend/app/routers/attendance.py` 由 **A、B 共同维护**——A 负责「查询类接口 + 删除记录 + 学生账号 `linked_student_id` 绑定」等与权限、数据范围相关的逻辑；B 负责 `POST /checkin` 及其中调用的活体、人脸比对、情绪写入等服务。

---

## 任家煜（A）：负责的文件（后端 / 数据库 / 系统安全）

### 核心与安全

| 路径 | 说明 |
|------|------|
| `backend/app/core/database.py` | SQLAlchemy 引擎与 Session |
| `backend/app/core/security.py` | 密码哈希、JWT 签发/校验、`require_roles`、`get_current_user` |
| `backend/app/core/student_visibility.py` | 学生端「仅本人数据」过滤条件 |
| `backend/app/core/upload_limits.py` | 图片/CSV 体积与魔数校验 |
| `backend/app/core/request_context.py` | 请求 ID 上下文 |
| `backend/app/core/timezone.py` | 时间格式化（如展示用） |

### 数据模型（ORM）

| 路径 | 说明 |
|------|------|
| `backend/app/models/user.py` | 用户表（含 `linked_student_id` 等与学籍绑定字段） |
| `backend/app/models/student.py` | 学生学籍 |
| `backend/app/models/__init__.py` | 模型导出 |
| `backend/app/db_schema.py` | SQLite 增量补列（迁移） |

其余业务表：`backend/app/models/attendance.py`、`group_photo.py`、`activity_log.py`、`emotion_log.py` 等表结构由 **A 主导定义**，**B** 在路由与服务中引用；若改字段需两人对齐。

### 路由与业务（主要负责）

| 路径 | 说明 |
|------|------|
| `backend/app/routers/auth.py` | 登录、`/me` |
| `backend/app/routers/students.py` | 学生 CRUD、人脸上传路径管理、CSV 批量导入 |
| `backend/app/routers/export_api.py` | 考勤 / 活动 Excel 导出（含学生维度过滤） |

### Schema

| 路径 | 说明 |
|------|------|
| `backend/app/schemas/auth.py` | 登录与用户信息 |
| `backend/app/schemas/student.py` | 学生相关请求/响应 |
| `backend/app/schemas/common.py` | 统一 `ApiResponse` 等（与全员约定） |

### 入口与脚本

| 路径 | 说明 |
|------|------|
| `backend/app/main.py` | CORS、静态目录挂载、`HTTPException`/校验/全局异常处理（与 B 协调：不影响识别路由行为即可） |
| `scripts/verify-auth.ps1` | 接口级鉴权冒烟（可选维护） |

### A 主导实现的 HTTP 接口（完整路径）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/login` | 登录 |
| GET | `/api/auth/me` | 当前用户 |
| GET | `/api/students` | 学生分页列表 |
| POST | `/api/students` | 新建学生 |
| PUT | `/api/students/{student_id}` | 更新（路径参数为路由定义的 `student_id`） |
| DELETE | `/api/students/{student_id}` | 删除 |
| POST | `/api/students/{student_id}/face` | 上传人脸图 |
| DELETE | `/api/students/{student_id}/face` | 删除人脸 |
| POST | `/api/students/batch-import` | CSV 批量导入 |
| GET | `/api/export/attendance/excel` | 考勤导出（结合筛选与学生权限） |
| GET | `/api/export/activity/excel` | 活动导出 |
| GET | `/health` | 健康检查（定义于 `main.py`，与 A 维护入口一致） |

### A 在 `attendance` 路由中的职责片段

- `GET /api/attendance/records`、`GET /api/attendance/sessions`：筛选条件、**学生仅看本人**（与 `student_visibility` 一致）。
- `DELETE /api/attendance/{record_id}`：教师删除记录。
- `POST /api/attendance/checkin` 中与「当前登录学生写入 `linked_student_id`」相关的用户更新逻辑（与 B 拆分在同一文件内，改代码前建议沟通）。

---

## 吴酽酌（B）：负责的文件（识别服务 + 对抗安全 + 考勤打卡 / 合照 / 情绪）

### 算法与服务层

| 路径 | 说明 |
|------|------|
| `backend/app/services/face_service.py` | DeepFace 特征提取、检测器回退、`represent` 等 |
| `backend/app/services/liveness_service.py` | MediaPipe/OpenCV 活体与纹理/频域等抗翻拍策略 |
| `backend/app/services/group_match_service.py` | 合照人脸检测与库比对 |
| `backend/app/services/emotion_service.py` | 情绪分析 |
| `backend/app/services/match_quality.py` | 比对阈值与间隔、降低误识 |

### 路由（主要负责）

| 路径 | 说明 |
|------|------|
| `backend/app/routers/photo.py` | 合照识别、合照列表、活动统计 |
| `backend/app/routers/emotion.py` | 情绪统计与明细 |

### 路由（协同）

| 路径 | 说明 |
|------|------|
| `backend/app/routers/attendance.py` | **主要负责** `POST /checkin` 全流程；与 A 分区维护同一文件，避免无关大改动 |

### Schema

| 路径 | 说明 |
|------|------|
| `backend/app/schemas/attendance.py` | 打卡响应、考勤记录项等 |
| `backend/app/schemas/photo.py` | 合照相关响应 |
| `backend/app/schemas/emotion.py` | 情绪统计与记录项 |

### 模型引用（只读/扩展字段与 B 协商）

考勤记录、情绪日志、合照等模型在 `backend/app/models/` 下以 **A 定表** 为准；B 侧重业务写入与查询字段含义文档化。

### B 主导实现的 HTTP 接口（完整路径）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/attendance/checkin` | 活体 → 人脸比对 → 写考勤 →（成功）情绪；学生首次成功绑定学籍 |
| POST | `/api/photo/recognize` | 合照识别并写库 |
| GET | `/api/photo/list` | 合照记录列表 |
| GET | `/api/photo/activity-stats` | 活动维度统计 |
| GET | `/api/emotion/stats` | 情绪分布 |
| GET | `/api/emotion/records` | 情绪明细分页 |

（上述接口均为教师或登录用户场景下的「识别与统计」能力；角色约束已在路由或依赖中体现，与 A 的 `security` 一致。）

---

## 巫威（C）：负责的文件（前端）

### 入口与路由

| 路径 | 说明 |
|------|------|
| `frontend/index.html` | 入口 HTML（若存在） |
| `frontend/src/main.js` | Vue 挂载、Element Plus |
| `frontend/src/App.vue` | 根组件 |
| `frontend/src/router/index.js` | 路由表、登录守卫、`/me` 拉取用户信息 |

### API 封装

| 路径 | 说明 |
|------|------|
| `frontend/src/api/client.js` | Axios 实例、拦截器、401、错误文案 |
| `frontend/src/api/auth.js` | 登录、`/me` |
| `frontend/src/api/attendance.js` | 打卡、考勤记录 |
| `frontend/src/api/students.js` | 学生管理与批量导入 |
| `frontend/src/api/photo.js` | 合照识别 |
| `frontend/src/api/emotion.js` | 情绪接口 |
| `frontend/src/api/export.js` | 导出下载 |

### 页面视图

| 路径 | 说明 |
|------|------|
| `frontend/src/views/LoginView.vue` | 登录 |
| `frontend/src/views/AttendanceView.vue` | 考勤打卡 |
| `frontend/src/views/RecordsView.vue` | 考勤记录查询 |
| `frontend/src/views/StudentManageView.vue` | 学生管理 |
| `frontend/src/views/PhotoRecognitionView.vue` | 合照识别 |
| `frontend/src/views/EmotionStatsView.vue` | 情绪统计 |

### 组件与布局

| 路径 | 说明 |
|------|------|
| `frontend/src/layouts/MainLayout.vue` | 主布局 |
| `frontend/src/components/layout/NavSidebar.vue` | 侧栏导航 |
| `frontend/src/components/layout/TopHeader.vue` | 顶栏、退出登录 |
| `frontend/src/components/CameraCapture.vue` | 摄像头采集 |

### 状态与工具

| 路径 | 说明 |
|------|------|
| `frontend/src/stores/groupPhotoRecognition.js` | 合照识别跨页面会话状态 |
| `frontend/src/utils/auth.js` | 前端角色判断等 |
| `frontend/src/styles/theme.css` | 主题样式 |

### C 对应的「页面 → 主要接口」映射（联调自查）

| 页面 | 主要调用的后端路径 |
|------|---------------------|
| 登录 | `POST /api/auth/login`、`GET /api/auth/me` |
| 考勤 | `POST /api/attendance/checkin` |
| 记录 | `GET /api/attendance/records`、`DELETE /api/attendance/{record_id}`（教师） |
| 学生管理 | `/api/students/*`、`POST /api/students/batch-import` |
| 合照 | `POST /api/photo/recognize`、`GET /api/photo/list` 等 |
| 情绪 | `GET /api/emotion/stats`、`GET /api/emotion/records` |
| 导出 | `GET /api/export/attendance/excel`、`GET /api/export/activity/excel` |

---

## 共建与文档

| 内容 | 建议 |
|------|------|
| `README.md` | 三人可读；环境与启动以 README 为准 |
| `face_attendance_outline.md` | 课程大纲对照验收 |
| OpenAPI | 后端启动后 `http://127.0.0.1:8000/docs`，接口字段以线上文档为准；分工中以路径为准，参数微调不逐一赘述 |

